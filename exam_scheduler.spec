# -*- mode: python ; coding: utf-8 -*-


block_cipher = None


a = Analysis(
    ['main.py'],
    pathex=[],
    binaries=[],
    datas=[('images/*', 'images'), ('credentials.json', '.'), ('back.png', '.'), ('days.png', '.'), ('delete.png', '.'), ('edit.png', '.'), ('even_sem.png', '.'), ('login1.png', '.'), ('logo.png', '.'), ('logo_1.png', '.'), ('no_internet.png', '.'), ('oddsem.png', '.'), ('pluss.png', '.'), ('refresh.png', '.'), ('seat_plus.png', '.'), ('select_venues.png', '.'), ('tick.png', '.')],
    hiddenimports=[],
    hookspath=[],
    hooksconfig={},
    runtime_hooks=[],
    excludes=[],
    win_no_prefer_redirects=False,
    win_private_assemblies=False,
    cipher=block_cipher,
    noarchive=False,
)
pyz = PYZ(a.pure, a.zipped_data, cipher=block_cipher)

exe = EXE(
    pyz,
    a.scripts,
    [],
    exclude_binaries=True,
    name='exam_scheduler',
    debug=False,
    bootloader_ignore_signals=False,
    strip=False,
    upx=True,
    console=True,
    disable_windowed_traceback=False,
    argv_emulation=False,
    target_arch=None,
    codesign_identity=None,
    entitlements_file=None,
)
coll = COLLECT(
    exe,
    a.binaries,
    a.zipfiles,
    a.datas,
    strip=False,
    upx=True,
    upx_exclude=[],
    name='exam_scheduler',
)
